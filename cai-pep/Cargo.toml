[package]
name = "cai-pep"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
shlex = "1"

[dev-dependencies]
tempfile = { workspace = true }
